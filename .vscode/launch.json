{
    "configurations": [
        {
            "type": "perl",
            "request": "launch",
            "name": "Perl Debug Configuration",
            "program": "${workspaceFolder}/${relativeFile}",
            "stopOnEntry": true
        }
    ]
}